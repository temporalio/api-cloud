version: v1
deps:
  - buf.build/googleapis/googleapis
breaking:
  use:
    - FILE
  ignore:
    - proto/api # from `api` root: do not lint public api
lint:
  use:
    - DEFAULT
  ignore:
    - proto/api # from `api` root: do not lint public api
