version: v1
deps:
  - buf.build/googleapis/googleapis
breaking:
  use:
    - FILE
build:
  excludes:
    - temporal-api/google
lint:
  use:
    - DEFAULT
  ignore:
    - temporal-api  # don't lint temporal-api

